#include "main.h"

/**
 * print_alphabet_x10 - a function that prints  the alphabet in lowercase
 * Return: 0
 */
void print_alphabet_x10(void)
{
	char j;
	int i;

	i = 0;

	while (i < 0)
	{j = 'a';
		while (j <= 'z')
		{
			_putchar(j);
			j++;
		}
		_putchar('\n);
		i++;
	}
}
